# Cloudflare Pages configuration
# This file is minimal and only contains build configuration
# Deploy is handled by Cloudflare Pages automatically

name = "orfoep-trainer"

# Build configuration for Cloudflare Pages
[build]
command = "pnpm install && pnpm build"
cwd = "./"
