# Полный промт для создания платформы "Орфоэпический Тренажер"

## Описание проекта

Создать полнофункциональную веб-платформу для подготовки к экзаменам с модульной системой тестирования. Платформа должна позволять пользователям проходить различные тесты, отслеживать свой прогресс и улучшать знания.

## Основные требования

### 1. Система аутентификации

**Требование:** Реализовать простую систему входа без паролей, основанную на нике (username).

**Функциональность:**
- Пользователь вводит свой ник на странице входа
- Если ника нет в базе данных - автоматически создается новый аккаунт
- Если ник существует - пользователь входит в свой аккаунт
- Все данные пользователя сохраняются в базе данных (не в localStorage)
- При входе обновляется время последнего входа (lastLoginAt)

**Технические детали:**
- Таблица БД: `user_accounts` с полями: id, nickname (уникальный), lastLoginAt, createdAt, updatedAt
- API endpoint: `users.loginByNickname` (POST)
- Вход: { nickname: "username" }
- Выход: { success: true, user: { id: 1, nickname: "username" } }

### 2. Главная панель (Dashboard)

**Требование:** После входа пользователь видит панель со всеми доступными модулями экзаменов.

**Функциональность:**
- Отображение списка модулей экзаменов в виде карточек
- На каждой карточке: название, описание, иконка, цвет фона
- Отображение статистики для каждого модуля: количество попыток, правильных ответов, процент точности
- Кнопка для запуска модуля
- Кнопка выхода из аккаунта

**Дизайн:**
- Адаптивная сетка (1 колонна на мобильных, 2-3 на десктопе)
- Темная тема (темный фон, светлый текст)
- Плавные переходы и анимации

**Модули (примеры):**
```javascript
[
  {
    id: 'orfoepiya',
    title: 'Орфоэпия',
    description: 'Правильное ударение в русских словах',
    icon: '🎯',
    color: 'from-blue-600 to-blue-800',
  },
  // Можно добавлять новые модули по аналогии
]
```

### 3. Орфоэпический тренажер (основной модуль)

**Требование:** Интерактивный тренажер для проверки знания правильного ударения в русских словах.

**Функциональность:**

#### Загрузка данных:
- Загрузить 267 русских слов с ударениями из файла `public/words.json`
- Структура слова: { word: "слово", stressed_word: "сло́во", stressed_vowel_index: 2 }
- Слова должны выбираться случайно

#### Интерфейс:
- Показать русское слово крупным шрифтом в центре экрана
- Под словом отобразить все гласные буквы как отдельные кнопки
- Гласные буквы должны быть выделены синим цветом в слове
- Остальные буквы серые

#### Взаимодействие:
- Пользователь нажимает на гласную букву, которая, по его мнению, ударная
- При нажатии кнопка меняет цвет (становится выделенной)
- Система проверяет ответ:
  - **Если правильно:** показать зеленую галочку, через 1.5 секунды автоматически перейти к следующему слову
  - **Если неправильно:** показать красный крест, отобразить правильный ответ (выделить правильную гласную зеленым), показать кнопку "Следующее слово"

#### Статистика:
- Отслеживать: всего попыток, правильных ответов, процент точности
- Отображать статистику в верхней части экрана
- Кнопка "Статистика" для просмотра подробных результатов
- Кнопка "Сброс" для обнуления статистики (опционально)
- Кнопка "Назад" для возврата на главную панель

#### Сохранение данных:
- Все результаты сохранять в базе данных в таблице `user_stats`
- Обновлять статистику в реальном времени при каждом ответе
- API endpoint: `stats.updateStats` (POST)
- Вход: { userId: 1, moduleId: "orfoepiya", totalAttempts: 10, correctAnswers: 7 }

### 4. База данных

**Требование:** Использовать MySQL для хранения всех данных пользователей и статистики.

**Таблицы:**

#### users (для OAuth интеграции)
```sql
CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  openId VARCHAR(255) UNIQUE,
  name VARCHAR(255),
  email VARCHAR(255),
  role VARCHAR(50) DEFAULT 'user',
  createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  lastSignedIn TIMESTAMP
);
```

#### user_accounts (для nickname-based аутентификации)
```sql
CREATE TABLE user_accounts (
  id INT PRIMARY KEY AUTO_INCREMENT,
  nickname VARCHAR(64) UNIQUE NOT NULL,
  lastLoginAt TIMESTAMP,
  createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### user_stats (для отслеживания статистики)
```sql
CREATE TABLE user_stats (
  id INT PRIMARY KEY AUTO_INCREMENT,
  userId INT NOT NULL,
  moduleId VARCHAR(64) NOT NULL,
  totalAttempts INT DEFAULT 0,
  correctAnswers INT DEFAULT 0,
  lastAttemptAt TIMESTAMP,
  createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (userId) REFERENCES user_accounts(id)
);
```

### 5. API endpoints

**Требование:** Реализовать tRPC API для всех операций.

**Authentication:**
- `auth.me` - получить текущего пользователя
- `auth.logout` - выход из аккаунта

**Users:**
- `users.loginByNickname` - вход/создание пользователя по нику

**Statistics:**
- `stats.getModuleStats` - получить статистику модуля (GET)
- `stats.getAllStats` - получить всю статистику пользователя (GET)
- `stats.updateStats` - обновить статистику (POST)

### 6. Frontend архитектура

**Требование:** Использовать React 19 с TypeScript, Tailwind CSS 4, shadcn/ui.

**Структура компонентов:**

```
client/src/
├── pages/
│   ├── Login.tsx          # Страница входа
│   ├── Dashboard.tsx      # Панель модулей
│   ├── Home.tsx           # Маршрутизация между страницами
│   └── NotFound.tsx       # 404 страница
├── components/
│   ├── StressGame.tsx     # Орфоэпический тренажер
│   ├── ErrorBoundary.tsx  # Обработка ошибок
│   └── ui/                # shadcn/ui компоненты
├── contexts/
│   ├── UserContext.tsx    # Контекст пользователя
│   └── ThemeContext.tsx   # Контекст темы
├── hooks/
│   └── useStressGame.ts   # Логика орфоэпии
├── lib/
│   └── trpc.ts            # tRPC клиент
├── App.tsx                # Корневой компонент
├── main.tsx               # Точка входа
└── index.css              # Глобальные стили
```

**Компоненты:**

#### Login.tsx
- Input поле для ввода ника
- Кнопка "Войти"
- Обработка ошибок
- Перенаправление на Dashboard после успешного входа

#### Dashboard.tsx
- Сетка модулей экзаменов
- Карточки модулей с статистикой
- Кнопка выхода
- Навигация к модулям

#### StressGame.tsx
- Отображение слова
- Кнопки для выбора гласных
- Обратная связь (правильно/неправильно)
- Отображение правильного ответа при ошибке
- Панель статистики
- Кнопки: "Статистика", "Сброс", "Назад"

#### useStressGame.ts (Custom Hook)
- Загрузка слов из JSON
- Выбор случайного слова
- Определение гласных букв
- Проверка ответа
- Отслеживание статистики
- Переход к следующему слову

### 7. Дизайн и UX

**Требование:** Создать современный, удобный интерфейс для мобильных и десктопных устройств.

**Цветовая схема:**
- Фон: темный (slate-900, slate-800)
- Текст: светлый (white, slate-300)
- Акценты: синий (blue-600, blue-700)
- Успех: зеленый (green-600)
- Ошибка: красный (red-600)

**Типография:**
- Заголовки: крупные, жирные
- Текст: четкий, легко читаемый
- Размер шрифта для мобильных: увеличенный

**Адаптивность:**
- Мобильный: 1 колонна, полная ширина
- Планшет: 2 колонны
- Десктоп: 3 колонны
- Все элементы должны быть легко нажимаемы на мобильных (минимум 44x44px)

**Анимации:**
- Плавные переходы между страницами
- Анимация при нажатии кнопок
- Анимация при смене слова
- Анимация при отображении результата

### 8. Тестирование

**Требование:** Написать unit тесты для критических функций.

**Тесты:**
- Тесты для API endpoints (создание пользователя, обновление статистики)
- Тесты для логики орфоэпии (определение гласных, проверка ударения)
- Тесты для расчета точности

**Инструменты:** Vitest

### 9. Развертывание

**Требование:** Подготовить проект для развертывания на Cloudflare Pages.

**Конфигурация:**
- wrangler.toml для Cloudflare
- Build command: `pnpm install && pnpm build`
- Build output directory: `client/dist`
- Поддержка переменных окружения

**Переменные окружения:**
```
DATABASE_URL=mysql://user:password@host/dbname
JWT_SECRET=secret-key
OAUTH_SERVER_URL=https://api.manus.im
VITE_APP_TITLE=Орфоэпический Тренажер
VITE_APP_LOGO=/logo.svg
```

## Технический стек

**Frontend:**
- React 19
- TypeScript
- Vite
- Tailwind CSS 4
- shadcn/ui
- Wouter (маршрутизация)
- tRPC (API клиент)

**Backend:**
- Node.js
- Express.js
- TypeScript
- tRPC
- Drizzle ORM

**База данных:**
- MySQL
- Cloudflare D1 (опционально)

**Тестирование:**
- Vitest

**Развертывание:**
- Cloudflare Pages
- GitHub

## Процесс разработки

### Фаза 1: Подготовка данных
1. Извлечь список русских слов с ударениями из документа
2. Преобразовать в JSON формат
3. Сохранить в `public/words.json`

### Фаза 2: Backend разработка
1. Создать схему БД (Drizzle ORM)
2. Реализовать функции работы с БД
3. Создать API endpoints (tRPC)
4. Написать unit тесты

### Фаза 3: Frontend разработка
1. Создать страницу входа (Login.tsx)
2. Создать контекст пользователя (UserContext.tsx)
3. Создать главную панель (Dashboard.tsx)
4. Создать компонент орфоэпии (StressGame.tsx)
5. Создать custom hook (useStressGame.ts)
6. Реализовать маршрутизацию (App.tsx)

### Фаза 4: Интеграция и тестирование
1. Интегрировать frontend и backend
2. Протестировать все функции
3. Оптимизировать производительность
4. Проверить на мобильных устройствах

### Фаза 5: Развертывание
1. Подготовить конфигурацию Cloudflare
2. Загрузить на GitHub
3. Развернуть на Cloudflare Pages
4. Проверить работоспособность

## Критерии успеха

✅ Пользователь может войти по нику без пароля
✅ Данные пользователя сохраняются в БД
✅ Пользователь видит панель с модулями
✅ Орфоэпический тренажер работает корректно
✅ Статистика сохраняется и обновляется
✅ При правильном ответе слово меняется автоматически
✅ При неправильном ответе показывается правильный ответ
✅ Интерфейс адаптивен для мобильных устройств
✅ Все unit тесты проходят
✅ Приложение развернуто на Cloudflare Pages

## Дополнительные функции (опционально)

- Сброс статистики
- Страница рейтинга пользователей
- Экспорт статистики в PDF/CSV
- Добавление новых модулей экзаменов
- Система достижений
- Уведомления о прогрессе
- Темный/светлый режим

## Документация

Должны быть созданы следующие документы:
- README.md - основная информация о проекте
- PROJECT_DOCUMENTATION.md - полная документация
- CLOUDFLARE_DEPLOYMENT.md - гайд развертывания
- CLOUDFLARE_QUICK_START.md - быстрый старт
- CREATION_PROMPT.md - этот файл

## Контакты и поддержка

GitHub репозиторий: https://github.com/NE-PABLO/orfoep-trainer

Для вопросов и предложений открывайте issues в репозитории.
